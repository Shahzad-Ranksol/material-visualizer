# Skills

No project-specific Claude Code skills exist for this repository. The general-purpose skills most relevant to working here:

- **`superpowers:test-driven-development`** — `server/` now has actual testable logic (auth, tenant-scoped material CRUD) for the first time in this project; there's still no test framework installed (see `CLAUDE.md`), so introducing one alongside TDD practice is worth considering for future backend work.
- **`superpowers:systematic-debugging`** — useful once the frontend gets wired to `server/`'s API (see `PROJECT.md` roadmap), since that introduces a new class of bugs (network/auth/CORS) this project hasn't had before.
